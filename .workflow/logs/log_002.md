## [LOG-003] Settings UI Refactoring and Footer Update
- **Date**: 2026-04-02
- **Agent**: Jules (Gemini 3 flash)
- **Session type**: UI Refactor / Feature Enhancement
- **What was done**:
  - Refactored the `SettingsScreen` to match the new design reference.
  - Implemented a new themed appearance picker (Light, Dark, System) using a grid-based layout.
  - Redesigned the "Storage" section with a custom card layout and "Change Saved Directory" button.
  - Styled the "AI Configuration" section with updated text fields and a primary action button.
  - Restored the "Gemma Model" text field to maintain functional parity.
  - Updated the footer with version info, credit to "Sajid", and a GitHub icon that links to the project repository.
  - Refactored `LeafBottomNav` with backdrop blur, custom styling, and a `rounded-t-32` design.
  - Ensured both components use dynamic theme colors for proper Light and Dark mode support.
  - Added `url_launcher` and `flutter_svg` dependencies.
- **Why**: Requested by the user to align the Settings UI with a specific design reference and update the GitHub redirection.
- **Files changed**:
  - `pubspec.yaml` — Added `url_launcher` and `flutter_svg`.
  - `lib/ui/screens/settings/settings_screen.dart` — Complete UI refactor with theme support and field restoration.
  - `lib/ui/widgets/leaf_bottom_nav.dart` — Navigation bar redesign with theme support.
- **Outcome**: The Settings UI and bottom navigation now match the design specifications while maintaining all existing functionality and theme flexibility.
- **References**: [DECISION-001], [DECISION-004]

## [LOG-004] Android Scoped Storage Permission Fix
- **Date**: 2026-04-02
- **Agent**: Antigravity
- **Session type**: Bug Fix
- **What was done**:
  - Added `permission_handler` to `pubspec.yaml`.
  - Added `MANAGE_EXTERNAL_STORAGE` and related properties to `AndroidManifest.xml`.
  - Added runtime storage permission checks in `settings_screen.dart` when selecting a new directory.
  - Updated "Change Saved Directory" button in `settings_screen.dart` to match `ElevatedButton.icon` UI styling.
  - Formally logged `bug_004.md` and `DECISION-009`.
- **Why**: The user reported that changing the directory prevents the creation of new books. This was caused by a lack of Android scoped storage write permissions for the newly chosen directory, causing `dart:io` to fail silently.
- **Files changed**:
  - `pubspec.yaml`
  - `android/app/src/main/AndroidManifest.xml`
  - `lib/ui/screens/settings/settings_screen.dart`
  - `.workflow/DECISIONS.md`, `.workflow/logs/log_002.md`, `.workflow/bugs/bug_004.md`
- **Outcome**: The app now requests storage permissions before updating the directory, fixing `dart:io` permission denied errors. The UI is also consistent.
- **References**: [DECISION-009], bug_004.md
